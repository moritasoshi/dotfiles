# dap

## Configuration

Install delve

```shell
go install github.com/go-delve/delve/cmd/dlv@latest
```

Install vscode-go
```shell
cd $HOME/src 
git clone https://github.com/golang/vscode-go
cd vscode-go
npm install
npm run compile
```
